# Login 2FA (React + TS + Styled Components + React Query)

Экран авторизации с 2-факторной проверкой. Реализованы моки API **через React Query** и все состояния: валидационные, сетевые, истечение кода, неверный код, успешная проверка.

## 🔐 Демо-данные

Email: demo@dev.io

Password: demo123

## ✨ Фичи

- Вход: email + password (валидация **Zod + React Hook Form**).
- Подсветка инпутов при фокусе, ошибки под полями.
- Переход на 2FA
- Таймер 30 сек, кнопка **Get new** при истечении.
- Ошибки 2FA: красные бордеры, `invalid code`, disabled **Continue**.
- Успех 2FA: `valid code`, кнопка **Continue** неактивна, таймер останавливается.

## 🧰 Стек

- **Vite + React + TypeScript**
- **styled-components v6**
- **@tanstack/react-query v5**
- **react-hook-form + @hookform/resolvers + zod**

## 🚀 Запуск

```bash
# Node 18+
npm i
npm run dev
```
